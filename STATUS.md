# وضعیت پروژه — ماژول چهره

آخرین به‌روزرسانی: ۱۴۰۵/۰۶/۰۳ (2026-08-25)

## فایل‌ها

| فایل | نقش |
|---|---|
| `face_demo.py` | **کدِ اصلی** — همهٔ منطق اینجاست (~۱۳۰۰ خط) |
| `Face_Mask_Demo.ipynb` | نوت‌بوکِ Colab — از روی `face_demo.py` ساخته می‌شود |
| `test_face_demo.py` | آزمونِ خودکار با مدل‌های ساختگی |
| `face_demo_v3_backup.py` | پشتیبانِ همین نسخه |
| `Face_Mask (1)_نسخه_قبلی_backup.ipynb` | نسخهٔ اولِ پروژه (v4) — دست‌نخورده |

نوت‌بوک از روی `face_demo.py` تولید می‌شود. **همیشه `face_demo.py` را
ویرایش کن**، بعد نوت‌بوک را دوباره بساز؛ وگرنه تغییرات از بین می‌رود.
(همین اشتباه یک بار باعث شد یک `build_classifier`ِ قدیمی بماند و
مدلِ اصلی اصلاً بارگذاری نشود.)

---

## چهار اصلاحِ این دور

### ۱) سرنخِ رنگیِ پوست خاموش شد — خطرِ حجاب

آزمونِ عینی:

| حالت | پیشانی | پایین | حکمِ سرنخ |
|---|---|---|---|
| بالاکلاوا | 0.00 | 0.00 | full_cover ✔ |
| **حجاب + ماسکِ پزشکی** | 0.12 | 0.04 | **full_cover ✘** |

حکمِ نهایی برای زنِ محجبه با ماسکِ پزشکی: **«دزد» با اطمینان ۱.۰۰**.

علت: سرنخ فرض می‌کند «پیشانیِ پوشیده = نیتِ مجرمانه». روسری هم پیشانی
را می‌پوشاند و هیچ راهی ندارد پارچهٔ روسری را از بالاکلاوا جدا کند.
در ایران یعنی سیستمی که مدام به زنانِ محجبه آلارم می‌دهد.

**اقدام:** `w_skin = 0.0` (پیش‌فرض). کد حذف نشده و برای گزارش در
کارتِ قضاوت محاسبه می‌شود، ولی در تصمیم دخالت ندارد.

### ۲) کلاسِ حجاب به پرامپت‌های صفر-شات اضافه شد

تفکیکِ نوعِ پوشش از سرنخِ رنگی به مدلِ صفر-شات منتقل شد، چون مدل
*معنا* می‌فهمد. دو گروه:

- `BENIGN` (۷ پرامپت): ماسکِ پزشکی، N95، **روسری، حجاب، چادر**، هودی
- `THREAT` (۵ پرامپت): بالاکلاوا، اسکی‌ماسک، صورتِ کاملاً پیچیده،
  کلاه‌کاسکتِ تمام‌صورت

### ۳) گیتِ ترکیبی — رفعِ آلارمِ کاذبِ نیم‌رخ

مشکل: در نیم‌رخ، عرضِ کادرِ سر از ۶۰ به ۲۱ پیکسل می‌افتاد (چون فاصلهٔ
دو چشم با `cos(زاویه)` کوچک می‌شود)، کادر روی مو و پس‌زمینه می‌افتاد و
سرنخ‌ها به‌غلط «پوشیده» می‌گفتند.

نسخهٔ اولِ پروژه این خطا را نداشت چون گیتش (`میانگین conf چشم/بینی ≥
0.5`) نیم‌رخ را **کاملاً** رد می‌کرد.

**ترکیب:** گیت فقط برای **حکمِ قرمز** اعمال می‌شود، نه برای دیدن.
- همه بررسی می‌شوند و می‌توانند نارنجی شوند.
- قرمز سه شرط دارد: آستانه + تعدادِ مشاهده + `best_frontal ≥ 0.45`.

`frontal_score` دو منبعِ مستقل را ضرب می‌کند:
- **کمینهٔ** اطمینانِ دو چشم (نه میانگین — میانگین فریب می‌خورد)
- نسبتِ فاصلهٔ چشم به عرضِ شانه (تقارن)
- ضربدر اطمینانِ چیرالیته

نتیجهٔ آزمون:

| زاویه | frontal | عرض کادر | قرمز مجاز؟ |
|---|---|---|---|
| روبه‌رو | 0.98 | 60px | ✅ |
| ۴۵ درجه | 0.98 | 27px | ✅ |
| ۶۵ درجه | 0.41 | 15px | 🚫 نارنجی |
| نیم‌رخ | 0.23 | — | 🚫 نارنجی |

به‌علاوه جبرانِ کوتاه‌شدگیِ کادر: `d = max(d, 0.135 × عرضِ شانه)`.

### ۴) رنگ‌بندیِ نسخهٔ اول برگشت

| وضعیت | BGR | برچسب |
|---|---|---|
| analyzing | (160,160,160) خاکستری | Analyzing... |
| clear | (0,200,0) سبز | Clear |
| suspicious | (0,140,255) نارنجی | Covered / Unclear |
| thief | (0,0,255) قرمز | SUSPICIOUS - ALERT |

---

## ✅ وضعیت آزمون‌ها

`python test_face_demo.py`  →  **همه سبز**

دو آزمونی که قبلاً شکست می‌خوردند، ایرادِ *خودِ آزمون* بودند: بعد از
اضافه‌شدنِ پارامترِ `frontal` به `decide()`، آزمون‌ها آن را پاس نمی‌دادند
و گیتِ قرمز — که درست کار می‌کرد — جلویشان را می‌گرفت. آزمون‌ها اصلاح
شدند و یک آزمونِ جدید هم اضافه شد:

| آزمون | نتیجه |
|---|---|
| حکم برای سه وضعیتِ معلوم | ✅ clear / suspicious / thief |
| صرفه‌جوییِ سرعت | ✅ ۶۲٪ کاهشِ فراخوانیِ طبقه‌بند (۱۸۰ → ۶۹) |
| زمان تا حکم | ✅ مشکوک ۲ فریم، دزد ۴ فریم |
| ★ گیتِ نیم‌رخ (جدید) | ✅ روبه‌رو و ۴۵° قرمز، نیم‌رخ نارنجی |
| چسبندگیِ قرمز | ✅ با ۰.۴s شواهدِ مخالف نمی‌شکند، با ۳s می‌شکند |
| گم‌نکردنِ هویت | ✅ بعد از عوض‌شدنِ شناسه، قرمز حفظ شد |
| پشت به دوربین | ✅ صفر مشاهده، صفر آلارم |
| خروجی‌ها | ✅ ویدیو، صفحهٔ قضاوت، JSON |

**آنچه هنوز آزموده نشده:** رفتار با مدل‌های *واقعی* روی ویدیوی واقعی.
آزمون‌ها با مدلِ ساختگی اجرا می‌شوند (منطق را می‌سنجند، نه دقتِ مدل).

## قدمِ بعدی

1. رفعِ دو آزمونِ شکست‌خورده (اول بررسی که ایرادِ آزمون است یا کد)
2. اجرای واقعی روی ویدیو در Colab و دیدنِ صفحهٔ قضاوت
3. تیونِ `red_min_frontal` بر اساس نتیجهٔ واقعی
