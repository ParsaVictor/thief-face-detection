# وضعیت پروژه — ماژول چهره

آخرین به‌روزرسانی: 2026-09-03 (تیونِ نهاییِ ردیاب)

## آخرین نسخهٔ کاری

**`Face_Mask_V7.ipynb`** ← این را اجرا کن. ۳۷ سلول، همه کامپایل می‌شود.

هیچ عددی را داخلِ کد عوض نکن — **همهٔ متغیرها در «سلولِ تنظیمات»**
درست بعد از سلولِ import هستند، با توضیحِ فارسی و جدولِ عیب‌یابی.

```python
POSE_WEIGHTS        = "yolo26s-pose.pt"
MIN_BRIGHT_FOR_SKIN = 50
MIN_SHARPNESS       = 19.0
RED_MARGIN          = 1.00     # سخت‌گیری خاموش (رفتار نسخهٔ اول)
ALERT_COOLDOWN      = 90
USE_ORIENTATION     = True

# ردیاب — این پنج عدد یک مجموعه‌اند، تکی عوضشان نکن
CONF_THRES          = 0.20     # = TRACK_LOW_THRESH باشد
MATCH_THRESH        = 0.60
TRACK_BUFFER        = 30
NEW_TRACK_THRESH    = 0.50
TRACK_HIGH_THRESH   = 0.50
TRACK_LOW_THRESH    = 0.20
```

## تاریخچه

| فایل | چیست |
|---|---|
| `Face_Mask (1)_نسخه_قبلی_backup.ipynb` | نسخهٔ اولِ اصلی — دست‌نخورده |
| `Face_Mask_V1_Clean.ipynb` | همان الگوریتم، منظم در ۸ بخش |
| `Face_Mask_V2_TRUE.ipynb` | + قفلِ نرمِ سبز، حداقل اندازه، رنگِ اسکلت |
| `Face_Mask_V3.ipynb` | + برشِ تمیز، اطمینان و JSON، مدلِ متغیر |
| `Face_Mask_V4.ipynb` | + حافظهٔ هویت، آهنگِ تطبیقی، تساویِ قطعی |
| `Face_Mask_V5.ipynb` | + ضدِ آلارمِ کاذب (D1..D4) + YOLO26 |
| **`Face_Mask_V7.ipynb`** | **+ فاز E مقاوم، تنظیماتِ یکجا، تیونِ ردیاب، گزارشِ هر شناسه** ← فعلی |

سازنده: `scratchpad/make_v7.py` (از V5 می‌سازد). الگوریتمِ فاز E در
`phase_E_orientation.py`. **نوت‌بوک را دستی ویرایش نکن** — سازنده را عوض کن.

---

## تازه در V7

### ۱) فاز E — «پشت به دوربین» (مقاوم‌شده)

ایده: کی‌پوینت‌های COCO برچسبِ **آناتومیک** دارند، پس
`x(شانهٔ چپ) − x(شانهٔ راست)` مثبت = روبه‌رو، منفی = پشت.
به دیده‌شدنِ صورت کاری ندارد → روی نقاب‌دار هم کار می‌کند.

**باگی که پیدا و رفع شد:** نسخهٔ اول گاهی به‌غلط «پشت» می‌گفت.
اندازه‌گیری نشان داد مقصر «سرنخِ دیده‌شدنِ صورت» بود:

| حالت | چشم | بینی | v_vis |
|---|---|---|---|
| صورتِ باز | 0.92 | 0.90 | +1.00 |
| ماسک‌دار | 0.12 | 0.05 | **−0.48** ⚠️ |
| نقابِ کامل | 0.02 | 0.02 | **−0.64** ⚠️ |

یعنی هر پوشیده‌صورتی به‌سمتِ «پشت» هل داده می‌شد.

پنج سخت‌سازی:
- **H1** سرنخِ دیده‌شدن **یک‌طرفه** شد (فقط می‌تواند به «روبه‌رو» هل بدهد)
- **H2** رأیِ شانه **الزامی** است
- **H3** توافقِ دو سرنخ یا شانهٔ قاطع
- **H4** هیسترزیس (ورود −۰.۳۵، خروج −۰.۲۰)
- **H5** شیرِ اطمینان: `ORI_SAFETY_VALVE = 45` — حتی اگر «پشت» بود،
  هر ۴۵ فریم به‌هرحال بررسی می‌شود؛ خطای پایدار نمی‌تواند کسی را پنهان کند

نتیجهٔ آزمون (۳۰۰ اجرای تصادفی در هر سناریو):

| سناریو | درست |
|---|---|
| نقاب‌دارِ روبه‌رو | ۱۰۰٪ |
| نقاب‌دار + شانهٔ ضعیف | ۱۰۰٪ |
| نقاب‌دار + هر سه مشکل | ۱۰۰٪ |
| نیم‌رخ ۶۰ درجه | ۱۰۰٪ (نیم‌رخ همچنان تحلیل می‌شود) |
| پشت به دوربین | ۱۰۰٪ |
| پشتِ ۳۰ درجه | ۱۰۰٪ |

هزینه: **۲۹ میکروثانیه/نفر** → ۵ نفر = ۰.۱۴ms = ۰.۳٪ یک فریم.
اثرِ خالص منفی است، چون برای فردِ پشت‌به‌دوربین طبقه‌بند اجرا نمی‌شود.

### ۲) تیونِ ردیاب — کاهشِ پرشِ شناسه

`tracker_custom.yaml` خودکار ساخته می‌شود:
`track_buffer` ۳۰→۹۰ (تحملِ انسداد ۱→۳ ثانیه)،
`new_track_thresh` ۰.۲۵→۰.۶۰ (تشخیصِ ضعیف شناسهٔ تازه نسازد).
هزینهٔ محاسباتی: **صفر**.

### ۳) سلولِ تنظیماتِ یکجا
~۳۵ متغیر با توضیح + جدولِ «اگر این مشکل را داری، این را عوض کن».

### ۴) گزارشِ هر شناسه
`ID | frames | front | prof | unk | back | checks | skipB | force | from | verdict`
به‌علاوهٔ «چرا پشت؟» (دلیلِ متنی) و خطِ زمانیِ حکم‌ها.

---

---

## ★ اصلاحِ نهایی — جابه‌جاییِ هویت هنگامِ عبور

**علامت:** یک نفر از جلوی دیگری رد می‌شد؛ نفرِ پشتی گم می‌شد و
حکمش (مثلاً «سبز») روی نفرِ جلویی (دزد) می‌نشست.

**علت:** تیونِ اولیهٔ ردیاب در V7 در جهتِ اشتباه بود.

| عدد | تیونِ اولیه | چرا غلط بود | نهایی |
|---|---|---|---|
| `match_thresh` | 0.85 | آستانهٔ *فاصلهٔ* IoU است نه شباهت. ۰.۸۵ ⇒ IoU>۱۵٪ کافی. سهل‌گیرتر از پیش‌فرض (۰.۸۰) شده بود | **0.60** (IoU>۴۰٪) |
| `track_buffer` | 90 | مسیرِ گم‌شده ۳ ثانیه با کالمن پیش‌بینی می‌شد و روی نفرِ کناری سُر می‌خورد | **30** |
| `new_track_thresh` | 0.60 | فردی که نیمه‌پنهان برمی‌گشت حق نداشت شناسهٔ تازه بسازد، پس جذبِ مسیرِ دیگری می‌شد | **0.50** |
| `conf_thres` | 0.40 | بالاتر از `track_low_thresh` بود، پس مرحلهٔ دومِ ByteTrack عملاً مرده بود | **0.20** |
| `track_low_thresh` | 0.10 | غیرقابل‌دسترس (زیرِ `conf_thres`) | **0.20** |

**درسِ اصلی:** «پایدارترکردنِ شناسه» و «جلوگیری از قاطی‌شدنِ هویت»
دو هدفِ **متضاد**اند. بافرِ بلند و تطبیقِ سهل‌گیر اولی را بهتر و
دومی را بدتر می‌کنند. اولویت با دومی است: پرشِ شناسه آزاردهنده
است، جابه‌جاییِ هویت خطرناک.

**راهِ درست برای پایداری** بالابردنِ بافر نبود، بلکه زنده‌کردنِ
مرحلهٔ دومِ ByteTrack بود: تشخیص‌های ضعیفِ ۰.۲۰–۰.۵۰ مسیرِ فردِ
نیمه‌پنهان را سرِ جای خودش نگه می‌دارند، پس مسیرش اصلاً آزاد
نمی‌شود تا به نفرِ دیگری منتقل شود.

**بررسیِ `TrackMemory`:** با `USE_TRACK_MEMORY = False` آزمون شد؛
خطا باقی ماند ⇒ مقصر نبود. روشن ماند.

---

## قدمِ بعدی

1. ✅ تیونِ ردیاب — تمام
2. آماده‌سازیِ مخزن و انتشار در گیت‌هاب
3. اگر بعداً «پشت» کاذب دیدی: `ORI_BACK_ENTER` را منفی‌تر کن (−۰.۵۰)
3. اگر شناسه‌ها هنوز می‌پرند: `botsort.yaml` با جبرانِ حرکتِ دوربین
4. فازِ بعدی: مچ/سلاح و آتش (طبق نقشهٔ اولیهٔ محصول)
